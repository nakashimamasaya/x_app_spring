## 概要

<!-- 何をしたか。1〜3 行で。 -->

## 対応フェーズ / スライス

<!-- 例: フェーズ3 / feat/post（投稿・削除・いいね） -->

- [ ] フェーズ 0: 土台整備
- [ ] フェーズ 1: 仕様策定
- [ ] フェーズ 2: テスト作成
- [ ] フェーズ 3: 実装
- [ ] フェーズ 4: 動作検証

## テスト結果

<!-- 実際に走らせた結果を貼る。「通るはず」は書かない。 -->

```
./gradlew :backend:test
docker compose run --rm web npm test
```

## ローカル LLM (qwen3:14b) 生成分

- [ ] なし（全て手書き）
- [ ] あり

生成したファイルと、レビューで修正した点:

<!--
例:
- backend/.../PostResponse.java — コンパクトコンストラクタ指定を無視していたので書き換え
-->

## チェックリスト

- [ ] `api/openapi.yaml` を変更した場合、型を再生成して契約テストを通した
- [ ] レスポンス DTO に `email` / `passwordHash` / `deletedAt` が混入していない
- [ ] 秘密情報をコミットしていない（gitleaks が green）
- [ ] LLM 生成コードは単独コミットに分けた

## 補足

<!-- レビューで見てほしい点、既知の制約、後続タスクなど -->
